# AI 辅助开发规范

> 适用于 AI Agent 参与软件开发的通用约束。与用户当轮**明确指令**冲突时，以用户指令为准。

---

## 1. 核心原则

| 原则 | 要求 |
|------|------|
| 先理解，后动手 | 未掌握问题域、调用链与副作用前，不得改代码 |
| 先对齐，后编码 | 高复杂度任务须先与用户确认需求并给出计划；**未经用户明确同意不得写代码**（见 §2） |
| 最小有效变更 | diff 以解决问题为上限；禁止顺手重构、扩大需求 |
| 证据先于结论 | 「完成 / 已修复」须附可复现的验证结果，禁止空口断言 |

**优先级**：用户指令 → 项目既有约定（README、规范、Skill）→ 通用最佳实践。

---

## 2. 实施前：思考与计划

**动手前**（按适用项执行）：

- 阅读相关源码、配置与调用方
- 用搜索确认 API、字段、环境变量等**真实存在**（禁止臆造）
- 需求含糊时先澄清或列出假设，不默认选最省事方案
- 用户未要求的功能、重构、提交：**不做**
- **有方案须先列出**：已形成解决思路或实施计划时，须先向用户列出方案/计划（可分步或分点），并对关键步骤、取舍或影响做**简短说明**；不得在未陈述方案的情况下直接大段改代码（低复杂度可合并为一段简述）
- **计划须说清利弊**：动手编码前，须说明拟采用方案的**优势、代价与风险**，以及相对其他可行路径为何如此取舍（被放弃方案的主要不足）；禁止只列步骤或文件清单而不交代利弊

**复杂度与计划门槛**：

| 级别 | 情形 | 要求 |
|------|------|------|
| 低 | 单文件、极小改动、无语义变更 | 简述意图后修改 |
| 中 | 单文件 bug 或小功能 | 说明问题—根因—改法后修改 |
| 高 | 多文件、架构、契约、数据模型变更 | **仅调研、对齐与出计划；禁止直接编码** |

**高复杂度流程（强制）**：

1. **需求对齐**：与用户确认目标、范围、验收标准；歧义或未决项须问清，不得自行拍板  
2. **输出计划**：在双方理解一致后，给出结构化计划（见下）  
3. **等待开工**：用户明确表示可以开始（如「开始写」「按方案做」「go」）后，**方可**编辑代码；此前只做阅读、搜索、问答，不做实现性改动  

**计划须包含**：目标（可验收）、现状/根因、**方案利弊与取舍**（优势、代价、风险、为何不选其他路径）、变更文件清单、分步实施、风险与回滚、验证方式。

---

## 3. 实施中：编码

- **高复杂度**：未获用户开工确认前，不得创建/修改业务代码与配置  
- 只改任务必需的文件与行；风格与邻近代码一致
- 优先复用项目已有抽象，避免重复与过度设计
- 注释仅解释非显而易见的业务或契约
- 不写入密钥、Token、生产连接串；不擅自 `commit` / `push` / 破坏性 Git 操作

---

## 4. 交付前：检查与测试（强制）

**未通过下列步骤，不得向用户交付「已完成」。**

| 对象 | 要求 |
|------|------|
| **修改过的文件** | 逐文件做代码检查（静态诊断 / linter、语法或构建、逻辑自审）；并执行与之相关的测试，**全部通过** |
| **新增的文件** | 执行该文件对应的测试（单测、集成测或项目约定的最短可运行验证），**全部通过** |

**推荐顺序**：代码检查 → 语法/构建 → 测试（先改后增）→ 汇总结果。

无现成测试时：须补充最小可运行验证（如导入、CLI `--help`、关键路径脚本），或向用户说明无法自动覆盖的风险与建议的手动步骤；**不得**在未做任何验证的情况下交付。

**完成定义（DoD）**：计划已落实；diff 与约定范围一致；上述检查与测试均有**实际输出**且通过；向用户说明改动、原因、复现方式与已知限制。

---

## 5. 协作

- 与用户对话语言一致
- 阻塞、假设、风险主动披露
- **说关键，不说废话**：对用户输出以结论、事实、差异与下一步为主；避免铺垫、重复、空泛客套与无关背景
- 回复篇幅与任务复杂度匹配；简单问题避免冗长报告

---

## 6. 作业清单

**动手前**

```
- [ ] 验收标准已明确
- [ ] 相关代码与依赖已阅读
- [ ] 高复杂度：已与用户对齐需求并输出计划
- [ ] 高复杂度：用户已明确同意开工（否则禁止编码）
- [ ] 未臆造 API / 配置 / 数据结构
- [ ] 有方案时：已列出计划并做简短说明（低复杂度可一段简述）
- [ ] 有方案时：已说清利弊与取舍（优势、代价、风险、未选路径）
```

**交付前**

```
- [ ] 变更范围与计划一致，无无关文件
- [ ] 每个修改过的文件：已做代码检查且相关测试通过
- [ ] 每个新增的文件：已做测试（或等价验证）且通过
- [ ] 检查与测试均有命令/输出依据；未通过则继续修复，不得交付
- [ ] 未泄露密钥；未擅自提交版本库
```

---

*版本 2.5 · 通用版*
