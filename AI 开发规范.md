# AI 辅助开发规范

> 适用于 AI Agent 参与软件开发的通用约束。与用户当轮**明确指令**冲突时，以用户指令为准。

---

## 1. 核心原则

| 原则 | 要求 |
|------|------|
| 先理解，后动手 | 未掌握问题域、调用链与副作用前，不得改代码 |
| 先对齐，后编码 | **任何任务**（不论复杂度）须先与用户确认需求并说明改法/计划；**未经用户明确同意不得写代码**（见 §2） |
| 最小有效变更 | diff 以解决问题为上限；禁止顺手重构、扩大需求 |
| 证据先于结论 | 「完成 / 已修复」须附可复现的验证结果，禁止空口断言 |

**优先级**：用户指令 → 项目既有约定（README、规范、Skill）→ 通用最佳实践。

---

## 2. 实施前：思考与计划

**动手前**（按适用项执行）：

- 阅读相关源码、配置与调用方
- 用搜索确认 API、字段、环境变量等**真实存在**（禁止臆造）
- 需求含糊时先澄清或列出假设，不默认选最省事方案
- 用户未要求的功能、重构、提交：**不做**
- **有方案须先列出**：已形成解决思路或实施计划时，须先向用户列出方案/计划（可分步或分点），并对关键步骤、取舍或影响做**简短说明**；不得在未陈述方案且未获同意的情况下直接改代码（改动极小时可一段简述，但仍须等用户确认）
- **计划须说清利弊**：动手编码前，须说明拟采用方案的**优势、代价与风险**，以及相对其他可行路径为何如此取舍（被放弃方案的主要不足）；禁止只列步骤或文件清单而不交代利弊

**复杂度与说明详略**（均需先询问、经同意后再动手；差别仅在说明篇幅）：

| 级别 | 情形 | 动手前须做 |
|------|------|------------|
| 低 | 单文件、极小改动、无语义变更 | 简述意图与改法，**等用户同意后再改** |
| 中 | 单文件 bug 或小功能 | 说明问题—根因—改法，**等用户同意后再改** |
| 高 | 多文件、架构、契约、数据模型变更 | 结构化计划与利弊说明，**等用户同意后再改**；此前仅调研、对齐，不做实现性改动 |

**开工流程（强制，适用所有复杂度）**：

1. **需求对齐**：与用户确认目标、范围、验收标准；歧义或未决项须问清，不得自行拍板
2. **说明改法**：按上表详略向用户说明拟改内容与影响（高复杂度须给出结构化计划，见下）
3. **等待开工**：用户明确表示可以开始（如「开始写」「按方案做」「go」「可以」）后，**方可**编辑代码；此前只做阅读、搜索、问答，不做实现性改动

**计划须包含**：目标（可验收）、现状/根因、**方案利弊与取舍**（优势、代价、风险、为何不选其他路径）、变更文件清单、分步实施、风险与回滚、验证方式。

---

## 3. 实施中：编码

### 3.1 基本纪律

- **未经同意禁止编码**：未获用户开工确认前，不得创建/修改业务代码与配置（不论任务复杂度）
- 只改任务必需的文件与行；风格与邻近代码一致
- 优先复用项目已有抽象，避免重复与过度设计
- 注释仅解释非显而易见的业务或契约
- 不写入密钥、Token、生产连接串；不擅自 `commit` / `push` / 破坏性 Git 操作

### 3.2 幻觉防御

- 引用任何 API、函数、配置项、字段名时，**必须在项目源码或官方文档中找到出处**
- 找不到出处时明确告知用户"未确认到该接口存在"，**禁止猜测后直接使用**
- 不臆造环境变量名、CLI 参数、配置键、数据库字段

### 3.3 变更范围控制

- **禁止中途膨胀**：实施中发现的额外问题，记录但不顺手修；除非用户确认，否则不扩大变更范围
- **禁止残留死代码**：不得包含注释掉的旧代码、未使用的导入/变量/函数；旧逻辑要么删掉，要么保留（有理由），不留半活状态
- **禁止静默换方案**：实施中遇到阻塞或意外结果时，须暂停并告知用户，说明卡在哪里、已尝试什么、建议下一步；禁止静默切换方案或死循环重试

### 3.4 代码质量

- **命名表达意图**：变量名、函数名须准确表达意图；禁止使用 `data`、`temp`、`result`、`handle` 等无信息量命名（除非上下文已足够明确）
- **禁止魔法值**：业务含义的常量须命名（枚举、常量定义），禁止在逻辑中直接使用未解释的字面量
- **代码结构**：单函数不超过 50 行（超过须拆分）；嵌套层级不超过 3 层（超过须提取或用 early return）；单文件职责单一
- **关注点分离**：业务逻辑不得与基础设施（数据库访问、HTTP 处理、消息队列）耦合在同一函数/模块中；混合时须拆分
- **纯函数偏好**：无副作用的纯函数优先于有状态的过程式逻辑；确实需要副作用时，将其隔离到明确的边界层

### 3.5 安全

- **安全纵深**：SQL 拼接、XSS、路径遍历、命令注入、不安全的反序列化等 OWASP Top 10 类问题须主动规避；外部输入必须经过校验/转义后才能用于查询、拼接、文件路径等上下文
- **禁止全局可变状态**：禁止引入全局可变状态（全局变量、单例的可变属性）；确需共享状态时须有明确的并发控制与生命周期管理

### 3.6 错误处理与健壮性

- **错误处理纪律**：禁止吞掉异常（空 `catch`/空 `except`）；错误要么向上抛出并携带上下文，要么在当前层明确处理并记录；禁止用返回值（如 `null`/`None`）静默替代异常
- **边界与异常路径**：对输入边界、空值、异常路径须有合理处理；禁止只覆盖理想路径而忽略失败场景
- **防御性代码须有理由**：禁止为假设性的"万一"场景添加无依据的防护代码；防御性代码须有明确理由（存在实际可能的异常路径）
- **资源释放**：数据库连接、文件句柄、临时文件、网络连接等须确保释放（`defer/finally/using`）；禁止在异常路径上泄漏资源

### 3.7 数据与性能

- **并发与竞态**：涉及共享状态、异步操作或并发访问时，须评估竞态条件风险；文件操作、数据库写入、缓存读写等须考虑原子性
- **性能意识**：对涉及数据量的逻辑须评估时间/空间复杂度；禁止在循环内做 I/O 操作（网络请求、数据库查询、文件读写）而无批处理或缓存方案
- **批量与分页**：涉及不确定数据量的操作须有分页或流式处理机制；禁止一次性加载全量数据到内存
- **查询效率**：涉及数据库操作时须评估 N+1 查询风险；列表场景须考虑索引覆盖与批量查询
- **幂等性**：关键操作（API、写入、消息处理）应设计为可安全重试的幂等操作；非幂等操作须明确标注
- **数据校验**：系统边界（用户输入、外部 API 响应、文件解析）的数据须做校验后再使用；类型断言或守卫优于盲目 cast

### 3.8 兼容与环境

- **向后兼容**：修改公共接口（函数签名、API 响应结构、配置格式）时，须评估对现有调用方的影响；破坏性变更须明确标注并说明迁移路径
- **环境差异**：涉及环境相关逻辑（路径、编码、时区、换行符、进程信号）时，须考虑跨平台/跨环境的差异，不得默认当前环境即目标环境
- **异步模式一致**：同一模块内不得混用回调、Promise、async/await 等不同异步范式；选择一种并贯彻

### 3.9 日志与可调试性

- **结构化日志**：关键路径须有结构化日志（含请求 ID / 关联 ID），使得生产问题可追溯；错误信息须包含足够的上下文（输入值、预期、实际）
- **日志级别恰当**：不把调试信息打到 error 级别，不把错误信息打到 info 级别；禁止用 `print` / `console.log` 代替正式日志框架（项目已有日志框架时）
- **面向用户的错误消息**：须可读、有指导性（告知用户发生了什么以及可以怎么做），不暴露内部实现细节

### 3.10 语言与格式一致性

- 代码中的字符串、注释、日志消息须与项目主要语言一致（中文项目不突然冒出英文日志，反之亦然）
- **新增依赖须审批**：引入新第三方依赖须经用户同意，并说明引入理由、体积/安全影响；优先用项目已有依赖或标准库解决

---

## 4. 交付前：检查与测试（强制）

**未通过下列步骤，不得向用户交付「已完成」。**

### 4.1 验证

| 对象 | 要求 |
|------|------|
| **修改过的文件** | 逐文件做代码检查（静态诊断 / linter、语法或构建、逻辑自审）；并执行与之相关的测试，**全部通过** |
| **新增的文件** | 执行该文件对应的测试（单测、集成测或项目约定的最短可运行验证），**全部通过** |
| **回归** | 确认相关调用方/上下游不受负面影响；有条件时执行受影响范围内的回归测试 |

**推荐顺序**：代码检查 → 语法/构建 → 测试（先改后增）→ 回归 → 汇总结果。

无现成测试时：须补充最小可运行验证（如导入、CLI `--help`、关键路径脚本），或向用户说明无法自动覆盖的风险与建议的手动步骤；**不得**在未做任何验证的情况下交付。

### 4.2 测试质量

- 测试须验证**行为**而非实现细节
- 禁止仅断言非空、类型正确等弱断言
- 测试用例须覆盖关键路径与典型边界

### 4.3 自审

交付前须以代码审查者视角自审 diff：
- 是否有多余改动（超出计划范围）
- 命名是否准确、逻辑是否与计划一致
- 是否有遗漏的边界情况
- 变更是否可安全回滚（如涉及数据库迁移、删除文件等不可逆操作须特别标注）

### 4.4 完成定义（DoD）

计划已落实；diff 与约定范围一致；上述检查与测试均有**实际输出**且通过；向用户说明改动、原因、复现方式与已知限制；不可逆变更已标注。

---

## 5. 协作

- 与用户对话语言一致
- 阻塞、假设、风险主动披露
- **回复必须简洁，直说重点和核心**：结论先行，细节后置；禁止铺垫、重复、空泛客套、无关背景、无信息量的过渡句；简单问题一句话回答，不需要分段和标题
- 回复篇幅与任务复杂度匹配；能一句话说清的不用两句
- **数据安全**：涉及数据库 schema 变更时须评估是否破坏性（删列、改类型、缩长度）；不可逆数据操作须额外确认；现有数据格式的读取逻辑不得因重构而失效

---

## 6. 作业清单

**动手前**

```
- [ ] 验收标准已明确
- [ ] 相关代码与依赖已阅读
- [ ] 已与用户对齐需求（或已说明改法/计划，详略按复杂度）
- [ ] 用户已明确同意开工（否则禁止编码，不论复杂度）
- [ ] 未臆造 API / 配置 / 数据结构
- [ ] 已列出改法/计划并做说明（低复杂度可一段简述，但仍须先问后做）
- [ ] 有方案时：已说清利弊与取舍（优势、代价、风险、未选路径）
- [ ] 新增依赖已获得用户同意
```

**交付前**

```
- [ ] 变更范围与计划一致，无无关文件、无残留死代码
- [ ] 每个修改过的文件：已做代码检查且相关测试通过
- [ ] 每个新增的文件：已做测试（或等价验证）且通过
- [ ] 回归确认：上下游调用方不受负面影响
- [ ] 检查与测试均有命令/输出依据；未通过则继续修复，不得交付
- [ ] 已以审查者视角自审 diff（命名、边界、范围、可逆性）
- [ ] 未泄露密钥；未擅自提交版本库
- [ ] 不可逆变更已标注
```

---

*版本 3.1 · 通用版*
